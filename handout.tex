\documentclass[10pt,a4paper]{article}

% Page setup
\usepackage[top=1.5cm, bottom=1.5cm, left=1.2cm, right=1.2cm]{geometry}
\usepackage{multicol}
\setlength{\columnsep}{1cm}

% Font & layout tools
\usepackage{graphicx}
\usepackage{parskip}
\usepackage{float}
\usepackage{caption}
\usepackage{microtype}
\usepackage{xcolor}

% No page numbering
\pagestyle{empty}

% Background
\usepackage{eso-pic}
\AddToShipoutPictureBG*{%
  \includegraphics[width=\paperwidth,height=\paperheight]{parchment.jpg}%
}

% Resizable logo with aspect ratio
\usepackage{tikz}
\newcommand{\headerlogo}[2][5cm]{%
  \begin{tikzpicture}[remember picture,overlay]
    \node at ([yshift=-0.7cm]current page.north) {\includegraphics[width=#1,keepaspectratio]{#2}};
  \end{tikzpicture}
}

% Font embedding (using TTF with pdflatex)
\usepackage[T1]{fontenc}
%\usepackage{fontspec} % doesn't work with pdflatex, but included for fallback
\usepackage{tgtermes} % fallback serif font
\usepackage{sectsty}

\newcommand{\loadmedievalfont}{%
  \renewcommand{\rmdefault}{ua1}%
  \sectionfont{\rmfamily\Large\bfseries}
  \subsectionfont{\rmfamily\large\bfseries}
}

% Manual font definition
\makeatletter
\newcommand{\embedmedievalfont}{
  \IfFileExists{fonts/blocuswebfont.ttf}{
    \pdfmapfile{=ua1.map}
    \DeclareFontFamily{T1}{ua1}{}
    \DeclareFontShape{T1}{ua1}{m}{n}{<-> [1.05] fonts/blocuswebfont.ttf}{}
    \loadmedievalfont
  }{}
}
\makeatother

% Image in column wrapper
\newcommand{\colimage}[2][]{%
  \begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth, #1]{#2}
  \end{figure}
}

\begin{document}

% Load header + medieval font
\headerlogo{aquila.png}
\embedmedievalfont
\vspace{0.5cm}

\begin{multicols}{2}

\section*{Mission: Faith & Flame}
Deep within the underhive, an unsanctioned preacher spreads rebellion through corrupted rites...

\colimage{hive_map.jpg}

\subsection*{Objectives}
\begin{enumerate}
  \item Silence the preacher
  \item Purge the heretic enclave
  \item Retrieve stolen relics
\end{enumerate}

\colimage[height=4cm]{relics.png}

\end{multicols}

\end{document}
